动态表单
  1、批量编辑属性及名称时 列表显示，查询显示在字段多的情况下关闭操作很麻烦（默认是打开的） 已优化
  2、动态表单生成后怎么迁移到生产环境，目前的处理方式是映射到生产环境提交数据，导出sql执行，动态表单多的时候该怎么办
  3、目前创建动态表单后需要手动去配置一遍角色，考虑在创建动态表单时就制定角色，后面可以在授权回收或者做更多处理。
  4、数据库列表查询清空不应该把请求提交到后端 已fix
  5、动态表达提交后页面没法刷新，一直转圈 未复现
  6、配置数据库时增加验证功能，验证账号密码和数据库是否正确 已fix
  7、增加数据库时，可以直接进行授权（待定）
   自定义事件传入参数，考虑是否需要增加智能提示
   table 组件需要支持 表头，列宽 行高设置, 需要尽快实现
   所有字段需要增加编辑是否禁用开关，以保护某些数据在编辑时不能被修改
   功能不停在累加，但是验证很少，需要增加不同场景的验证
   后期计划（计划明年内完成）：
     接入审批流程，实现数据自动采集 （数据库，软硬件系统，第三方接口）
 持续集成（开发计划，计划今年内实现初版编译部署）
   1、创建构建配置，制定项目信息，代码块信息，部署信息，项目成员信息，项目质量信息
   需加大力度把此模块给开发并完善
 运维自动化（计划最迟2026年完成）：
   1、设备纳管
   2、运维自动化
   3、安全加固
   4、软件安装
   5、OS安装（没有环境支持）
   6、监控预警
      主要是宕机，高负载，软硬件损坏和磁盘容量
 数据库查询
    1、授权需要优化，按角色或按用户授权 改为选择（支持多选）
 Bug:
    点击浏览器返回按钮，页面出现无限加载 已fix
    动态列表下拉组件没法选择，出现多个被选择的情况 没有复现
    下拉组件默认值是字符，当数据库字段定义为数字时保存报错，已fix
     动态列表组件切换困难 已fix
     目前动态表单复杂模型下，存在插入数据异常问题，主要表现在，如：折叠表单下某个折叠项，定义了一个列表，没有额外其它字段，这个时候批量数据没法入库 已fix
    系统模块：
      1、应用权限 选择子应用时，在系统内看不到，必须要手动加入父应用 已fix
      2、消费视图还不完善，需要进一步优化
      3、tselect 组件 change 事件不能触发，目前找不到原因 已fix 动态v-on 存在缺陷
      4、数据联动问题
      5、表单设计，容器现在不能调整行
      6、switch 开关长度现在是100
      7、select 目前页面没有显示长度




