动态表单一对多时，配置的集合名称要和实体类的集合名称一致，否则会报错。
动态表单需要增加是否阻止初始化时触发属性配置的事件。
textarea 组件需要增加行数的默认值

动态表单分配权限的地方需根据权限获取菜单信息

开发工作遗留项
1、首页配置没有与后端联动，已完成与后端对接，但对应模块还没有数据填充
2、流程模块没有与后端联调，前端页面目前还不完善需要继续优化处理，对于流程审批，需要有专门的审批模块以应对各种审批场景。
3、消息流程处理没有与后端联动，后端无接口对接
   备注：
      目前已打通表单对接，后续只需绑定对应的表单数据和审批信息
4、全局查询前端没有设置结果跳转，没有与后端联动，后端无接口对接
5、日程模块后期优化后没有验证
6、持续集成模块，一直处于绑定表单状态，没有与后端联调，也没有具体驱动代码编译发布等操作
   备注：
      持续集成目前一个节点要求必须配置一个基础属性和选配高级属性。
      节点切换时涉及联动的数据还存在bug
7、运维模块后端代码开发完成，但没有前端页面
8、动态页面目前只有静态页面，部分功能还不完善，没有与后端进行数据交互，后端无接口对接。
   备注：
     近期需要进一步完善动态页面配置，后续依赖于该模块的业务才能正常开展
9、新起的MES系统，目前也处于停滞状态。
   备注：
      此项目怎么快速的迭代起来？需要仔细考量。
      
目标：今年尽可能完成上述模块的迭代，以便后面可以投入到数据安全相关的学习中去。
Bug总结：
  在star-horse-form 中假如表单数据对象为formData, 不要在使用outerFormData="formData"后属性，还使用表单setFormData(formData["subData"])进行赋值，
  这样会导致数据层级改变，formData的数据最终变成subData的子节点，
