# StarHorseDevops UI

管理平台前端开发备忘录

* 开发进度
* ----------------------近期目标---------------------------------
* 属性拖好后，直接批量修改，label，name,是否必填，显示位置，解决当属性多的时候查找困难问题 已完成
  目前下拉框，树形下拉框，穿梭框 数据源问题没解决 已完成
  子表单继承父表单问题没解决 已完成
  使用其它数据源创建表没解决 已完成
  动态表单高级属性涉及到得事件还没有驱动起来
  页面需以设计风格展示 （已实现简单展示，但未加入设计的全部属性，目前只有标签，名字，是否必须） 已完成
  以上计划1个月
  在这过程中增加了nosql 数据库，关系型数据库的支持，新增新模块后，该功能待进一步测试
  同时增加了对动态视图查询的创建和消费
  目前动态视图还需要增加导出功能，查询还需要进一步完善 ，同事动态视图也需要创建菜单，以满足内部系统消费，
  动态表单完成，需要对接运维模块以实现数据的自动采集。&该功能强关联运维模块
* --------------------完成上述功能后-----------------------------------
* 系统模块部分bug 修复
  表单风格 tab 格式（tab1 展示表单 tab2 展示列表）， form和table 左右显示或上下显示（左边展示表单右边展示列表，或者上面展示表单下面展示列表），单独表单页面
  计划 1个月
  页面与流程绑定 计划1个月
  流程设计与流程创建节点并存模式 计划一个月
  后期加入编译流水线 待定
  运维。。。
* 系统管理

> 已完成权限配置，操作审计等功能，前后端可投入生产使用，当前存在角色配置和角色权限在一个页面完成，耦合度较高，考虑以系统为维度，对权限重新赋权

* 持续集成

> 待开发，计划功能 编译构建（技术选型上是去Jenkins化还是利用Jenkins，目前没有答案），系统部署（获取构建后的输出文件，或者手动配置软件位置）

* 动态建模

> 已实现前后端动态创建数据模型闭环，支持sql和nosql 数据库；需加入数据消费配置和远程数据源(需要和dbsearch模块对接)
> 已实现动态消费视图，待完善 视图生成页面，视图权限控制
> 现需要实现业务关联，如果业务表配置了映射关系，则后台解析对应的属性，并按照对应关系做映射处理（待实现）
> 在配置多对多关系时，需要生成第三方映射表（待实现）
> 待实现与运维模块对接实现数据的自动采集，

* 在线运维

> 待开发，健康巡检（服务器，数据库，网络设备），合规检测（数据库配置，服务器配置），安全检测（数据库安全，服务器安全和软件安全），
> 数据采集（主要作为后台功能使用，主要由动态建模驱动），
> 软件安装和系统部署（作为持续集成的下游逻辑，将具体的软件或者应用部署到指定目标设备）

* 动态工作流配置

> 已实现后台业务接口包括创建流程，驱动流程执行和获取个人任务列表，前端目前引入了jbpm-js 待开发具体逻辑，规划功能，将流程加入数据建模

* DbSearch

> 此模块已完成数据库配置，数据库操作授权和在线查询
> 待实现在该模块实现数据库备份

* 低代码（根据数据模型生成业务代码）

> 目前该功能后端已完成后端业务，前端功能待开发。

## 功能描述

展望，但愿在后期能加入容器和大数据相关功能

### 数据

#### 常见问题

* 刷新浏览器页面丢失问题

> location / {
> try_files $uri $uri/ @router;
> index index.html index.htm;
> }
> #对应上面的@router，主要原因是路由的路径资源并不是一个真实的路径，所以无法找到具体的文件
> #因此需要rewrite到index.html中，然后交给路由在处理请求资源
> location @router {
> rewrite ^.*$ /index.html last;
> }
>
>
>
> codemirrors 升级 升级前依赖备份：
"codemirror": "5.64.0",
"codemirror-editor-vue3": "^2.3.0",
> js-beautify": "^1.14.7",
> dev 依赖
"@codemirror/buildhelper": "^1.0.0",
"@types/codemirror": "^5.60.9",
"@types/js-beautify": "^1.14.0",
>
>  "@codemirror/gutter": "^0.19.9",
"@codemirror/history": "^0.19.2",
"@codemirror/lang-java": "^6.0.1",
"@codemirror/lang-javascript": "^6.2.1",
"@codemirror/lang-python": "^6.1.3",
"@codemirror/lang-sql": "^6.5.4",
"@codemirror/theme-one-dark": "^6.1.2",
"codemirror": "^6.0.1",
##package 中插件版本检测工具
npm install -g npm-check-updates
ncu -- 检查最新版本
ncu -u  更新版本
ncu -m  检查devDependencies 中的版本
## 开发清单

1、目前系统没有对外提供网关接口，第三方数据没法通过http方式或者MQ方式进来；
如果开发此功能，需考虑字段映射，以及业务关系映射（一个接口处理关联表的数据信息）
2、当前运维模块需要由Python实现，由于是第一个即将实践的Python项目，技术选型需要做更多调研
3、Ansible 目前处于半吊子水平，对Windows这块的管理目前还没有调通，
4、虽然兼容性还不错，数据量大的时候怎么考虑数据的查询性能
5、动态建模，已支持mysql/mariadb,postgrel,oracle,sqlserver，目前关联数据还没有处理
6、现有后端需要构建一个企业总线模块为第三方接口交互做解耦，后期无需刻意去处理已有的业务模块
7、对于流水线，虽然在规划内，但是近期可能无法去实现
8、手动部署工具需进一步完善，JDK升级后不能正确打印日志
9、流程引擎 虽然有简单的实现，但没有具体的业务绑定，后面需要与动态建模绑定起来
10、脚本，这属于是运维的必须品，需要和运维模块同步进行
11、数据库在线备份需要进一步处理
###知识点
动态绑定事件 v-on:["事件名称"]